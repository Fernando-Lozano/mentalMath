const navLinks = document.querySelectorAll(".nav-link");

navLinks.forEach(link => {
  link.addEventListener("click", function(e) {
    e.preventDefault();

    // toggle active
    // toggle last active
    // point last active
    // change number operator visually and programmatically
        // on change, reset calculator
  });
});